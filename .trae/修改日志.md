---

## [2026-04-05] - 阵容助手自动刷新与操作速度优化

### 文件：`src/components/cards/Unlimitedlineup.vue`, `src/utils/xyzwWebSocket.js`

#### 修改类型：Bug修复与优化

- **阵容助手自动刷新**：
  - 修复应用阵容后不自动刷新数据的问题，现在应用阵容后会自动显示最新阵容数据
  - 简化 `applyLineup` 函数的响应式更新逻辑，与备份版本保持一致
  - 确保数据更新后正确触发组件重新渲染

- **操作速度控制**：
  - 在调用 `refreshTeamInfo()` 之前添加适当的延迟，避免触发服务器的“操作速度过快”限制
  - 确保命令执行间隔合理，提高操作稳定性

- **科技配置同步**：
  - 修复科技类型映射错误，将遍历范围从 `[1, 2, 3, 4, 5, 6]` 改为 `[1, 2, 3, 4]`
  - 添加 `if (techIds)` 检查，确保只有当映射存在时才进行迭代
  - 为所有相关的遍历操作添加安全检查，提高功能稳定性

- **WebSocket命令注册**：
  - 清理 `xyzwWebSocket.js` 中重复的 `presetteam_getinfo` 命令注册
  - 确保 WebSocket 通信正常，避免命令处理异常

**关键代码修改：**

1. **Unlimitedlineup.vue - 应用阵容后自动刷新**：
```javascript
// 修改前
lastRefreshTime = 0;
await refreshTeamInfo();
// 确保数据更新后重新渲染
await delay(200);
// 强制清空编辑状态，确保使用最新的阵容数据
editingTeamHeroes.value = {};
// 触发响应式更新
currentTeamId.value = currentTeamId.value;

// 修改后
lastRefreshTime = 0;
await delay(COMMAND_DELAY);
await refreshTeamInfo();
```

2. **Unlimitedlineup.vue - 科技配置同步修复**：
```javascript
// 修改前
for (const type of [1, 2, 3, 4, 5, 6]) {
  const techIds = LEGION_TECH_RESET_TYPE_MAP[type];
  for (const techId of techIds) {
    // 处理逻辑...
  }
  const techIds2 = LEGION_TECH_TYPE_MAP[type];
  for (const techId of techIds2) {
    // 处理逻辑...
  }
}

// 修改后
for (const type of [1, 2, 3, 4]) {
  const techIds = LEGION_TECH_RESET_TYPE_MAP[type];
  if (techIds) {
    for (const techId of techIds) {
      // 处理逻辑...
    }
  }
  const techIds2 = LEGION_TECH_TYPE_MAP[type];
  if (techIds2) {
    for (const techId of techIds2) {
      // 处理逻辑...
    }
  }
}
```

3. **xyzwWebSocket.js - 清理重复命令注册**：
```javascript
// 修改前
// 队伍相关
.register("presetteam_getinfo")
.register("presetteam_getinfo")
.register("presetteam_setteam")

// 修改后
// 队伍相关
.register("presetteam_getinfo")
.register("presetteam_setteam")
```

---

## [2026-04-04] - 阵容助手功能完整实现与优化

### 文件：`src/utils/xyzwWebSocket.js`, `src/components/GameStatus.vue`, `阵容助手命令添加文档.md`, `backup_20260402_2217_优化导出配置显示效果与备份版本一致/src/utils/xyzwWebSocket.js`

#### 修改类型：功能完善与优化

- **WebSocket命令注册完整补全**：
  - 添加阵容助手基础命令：`hero_exchange`（武将交换）、`hero_gointobattle`（武将上阵）、`hero_gobackbattle`（武将下阵）、`artifact_load`（鱼灵装备）
  - 添加阵容助手高级命令：`hero_heroupgradelevel`（武将升级）、`hero_heroupgradeorder`（武将进阶）、`hero_rebirth`（武将重生）
  - 添加阵容助手装备命令：`artifact_unload`（鱼灵卸下）、`lordweapon_changedefaultweapon`（切换玩具）、`pearl_replaceskill`（鱼珠技能替换）、`pearl_exchangeskill`（鱼珠技能交换）、`pearl_unloadskill`（鱼珠技能卸下）
  - 添加车辆功能完整命令：`car_claimpartconsumereward`（车辆零件消耗奖励）
  - 添加军团命令：`legion_resetresearch`（重置俱乐部研究）
  - 添加活动命令：`activity_buystoregoods`（活动商店购买）
  - 解决 `Unknown cmd` 错误导致服务器断开连接的问题

- **响应映射完善**：
  - 在 `syncresp` 数组中添加：`hero_gointobattle`、`hero_gobackbattle`、`lordweapon_changedefaultweapon`
  - 在 `syncrewardresp` 数组中添加：`hero_exchange`、`hero_rebirth`、`activity_buystoregoods`
  - 确保命令响应能正确匹配

- **客户端版本号更新**：
  - 将客户端版本从 `2.10.3-f10a39eaa0c409f4-wx` 更新到 `2.21.2-fa918e1997301834-wx`
  - 与备份版本保持一致，确保兼容性

- **备份版本同步**：
  - 在备份版本中添加 `activity_buystoregoods` 命令注册
  - 确保本地版本与备份版本功能完全一致

- **GameStatus.vue 显示优化**：
  - 将阵容助手（Unlimitedlineup）的 `v-if` 改为 `v-show`，保持组件状态
  - 避免频繁重新初始化，提升用户体验

- **文档更新**：
  - 更新 `阵容助手命令添加文档.md`，补充武将升级相关命令说明
  - 在命令注册、功能说明、检查清单等部分添加完整信息

**关键代码修改：**

1. **xyzwWebSocket.js - 命令注册**：
```javascript
// 角色/系统
.register("role_getroleinfo", {
  clientVersion: "2.21.2-fa918e1997301834-wx",  // 更新版本号
  inviteUid: 0,
  platform: "hortor",
  platformExt: "mix",
  scene: "",
})

// 军团匹配和签到
.register("legionmatch_rolesignup")
.register("legion_signin")
.register("legion_resetresearch")  // 新增：重置俱乐部研究

// 队伍相关
.register("presetteam_getinfo")
.register("presetteam_setteam")
.register("presetteam_saveteam", { teamId: 1 })
.register("role_gettargetteam")
.register("hero_exchange")        // 新增：武将交换
.register("hero_gointobattle")    // 新增：武将上阵
.register("hero_gobackbattle")    // 新增：武将下阵
.register("artifact_load")        // 新增：鱼灵装备
.register("artifact_unload")      // 新增：鱼灵卸下
.register("lordweapon_changedefaultweapon")  // 新增：切换玩具
.register("pearl_replaceskill")   // 新增：鱼珠技能替换
.register("pearl_exchangeskill")  // 新增：鱼珠技能交换
.register("pearl_unloadskill")    // 新增：鱼珠技能卸下

// 武将升级相关
.register("hero_heroupgradelevel") // 新增：武将升级
.register("hero_heroupgradeorder") // 新增：武将进阶
.register("hero_rebirth")          // 新增：武将重生

// 车辆相关
.register("car_claimpartconsumereward") // 新增：车辆零件消耗奖励

// 活动/任务
.register("activity_get")
.register("activity_recyclewarorderrewardclaim")
.register("activity_buystoregoods")  // 新增：活动商店购买
```

2. **xyzwWebSocket.js - 响应映射**：
```javascript
syncresp: [
  "system_mysharecallback",
  "task_claimdailypoint",
  "role_commitpassword",
  "hero_gointobattle",           // 新增
  "hero_gobackbattle",           // 新增
  "lordweapon_changedefaultweapon", // 新增
],
syncrewardresp: [
  "system_buygold",
  "discount_claimreward",
  "card_claimreward",
  "artifact_lottery",
  "genie_sweep",
  "genie_buysweep",
  "system_signinreward",
  "dungeon_selecthero",
  "artifact_exchange",
  "hero_exchange",               // 新增
  "hero_rebirth",                // 新增
  "activity_buystoregoods",      // 新增
],
```

3. **GameStatus.vue**：
```html
<!-- 修改前 -->
<Unlimitedlineup v-if="activeSection === 'tools'" />

<!-- 修改后 -->
<Unlimitedlineup v-show="activeSection === 'tools'" />
```

4. **阵容助手命令添加文档.md**：
- 在"需要添加的命令"部分补充武将升级命令
- 在"命令功能说明"表格中添加功能描述
- 在"检查清单"中添加相关检查项

---

## [2026-04-03] - 阵容助手导入乱码问题修复

### 文件：`src/components/cards/Unlimitedlineup.vue`

#### 修改类型：Bug修复

- **导入乱码问题修复**：
  - 修复阵容助手导入时中文字符显示乱码的问题
  - 调整解码逻辑，使其与导出时的编码逻辑对称
  - 确保账号名称等中文信息能够正确显示

- **技术实现**：
  - 导出时使用：`btoa(unescape(encodeURIComponent(jsonString)))`
  - 导入时使用：`decodeURIComponent(escape(atob(content)))`
  - 保持编码和解码过程的对称性

**关键代码修改：**

1. **Unlimitedlineup.vue**：
```javascript
// 修改前
const decodedContent = atob(content);
importData = JSON.parse(decodedContent);

// 修改后
const decodedContent = decodeURIComponent(escape(atob(content)));
importData = JSON.parse(decodedContent);
```

---

## [2026-04-03] - 版本回退与阵容助手导出功能修复

### 文件：`version.json`, `package.json`, `android/app/build.gradle`, `src/components/cards/Unlimitedlineup.vue`

#### 修改类型：版本回退与功能修复

- **阵容助手导出功能修复**：
  - 增强环境检测：添加 `isFilesystemAvailable` 函数，实时检查 Filesystem API 是否可用
  - 优化权限处理：兼容不同 Capacitor 版本的权限属性（`publicStorage` 和 `storage`）
  - 增强错误处理：为文件写入操作添加专门的错误捕获
  - 确保稳定性：多层 try-catch 结构，任何错误都不会影响导出功能
  - 自动回退机制：任何步骤失败都会自动回退到浏览器下载方式

- **版本回退**：
  - 将APK版本从3.26.0回退到3.25.18
  - 更新version.json: versionCode 32518, versionName "3.25.18"
  - 更新package.json: version "3.25.18"
  - 更新build.gradle: versionCode 32518, versionName "3.25.18"

- **build.gradle 修复**：
  - 恢复完整的 build.gradle 文件内容
  - 添加缺失的 dependencies 部分
  - 添加 capacitor.build.gradle 引用

**关键代码修改：**

1. **Unlimitedlineup.vue**：
```javascript
// 检查Filesystem API是否可用
const isFilesystemAvailable = () => {
  try {
    return typeof window !== 'undefined' && 
           window.Capacitor && 
           window.Capacitor.Plugins && 
           window.Capacitor.Plugins.Filesystem;
  } catch (e) {
    console.warn('检测Filesystem API失败:', e);
    return false;
  }
};

// 通用文件保存函数（增强版）
const saveFile = async (jsonString, fileName, successMessage, loadingMsg) => {
  try {
    const androidApp = isAndroidApp();
    const filesystemAvailable = isFilesystemAvailable();
    const currentFs = filesystemAvailable ? window.Capacitor.Plugins.Filesystem : fsFilesystem;
    
    if (androidApp && currentFs) {
      try {
        // 检查存储权限（兼容不同Capacitor版本）
        let permStatus;
        try {
          permStatus = await currentFs.checkPermissions();
        } catch (permError) {
          return { success: false, isBrowser: true };
        }
        
        // 处理不同权限属性名
        const hasPermission = permStatus.publicStorage === 'granted' || 
                             permStatus.storage === 'granted';
        
        if (!hasPermission) {
          try {
            const reqResult = await currentFs.requestPermissions();
            const reqHasPermission = reqResult.publicStorage === 'granted' || 
                                    reqResult.storage === 'granted';
            
            if (!reqHasPermission) {
              return { success: false, isBrowser: true };
            }
          } catch (reqError) {
            return { success: false, isBrowser: true };
          }
        }

        // 准备数据并保存文件
        const encodedData = btoa(unescape(encodeURIComponent(jsonString)));
        try {
          await currentFs.writeFile({
            path: fileName,
            data: encodedData,
            directory: Directory.Documents,
            encoding: 'utf8'
          });
          
          return { success: true, path: `/storage/emulated/0/Documents/${fileName}` };
        } catch (writeError) {
          return { success: false, isBrowser: true };
        }
      } catch (error) {
        return { success: false, isBrowser: true };
      }
    } else {
      return { success: false, isBrowser: true };
    }
  } catch (error) {
    return { success: false, isBrowser: true };
  }
};
```

2. **build.gradle**：
```gradle
dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"
    implementation project(':capacitor-android')
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
    implementation project(':capacitor-cordova-android-plugins')
}

apply from: 'capacitor.build.gradle'
```

---

## [2026-04-03] - 改进阵容助手导出导入功能

### 文件：`src/components/cards/Unlimitedlineup.vue`

#### 修改类型：功能改进

- **APK 环境支持**：
  - 在 Unlimitedlineup.vue 中添加 Capacitor Filesystem API 导入
  - 修改 `exportLineups` 函数，添加 APK 环境的导出支持
  - 实现存储权限请求和文件保存到 Documents 目录
  - 添加浏览器下载的回退方案
  - 添加通用APK环境检测函数 `isAndroidApp()`
  - 添加通用文件保存函数 `saveFile()`
  - 添加浏览器下载函数 `saveFileByDownload()`

- **导入功能改进**：
  - 改进 `importLineups` 函数，添加 Base64 编码文件支持
  - 添加加载提示，提升用户体验
  - 添加详细的错误处理和用户提示
  - 实现 JSON 和 Base64 格式的自动识别

- **用户体验提升**：
  - 为导出导入操作添加加载提示
  - 显示详细的文件保存路径
  - 提供清晰的错误信息和解决建议
  - 支持多种文件格式，提高兼容性

**关键代码修改：**

1. **Unlimitedlineup.vue**：
```javascript
// 导入 Capacitor Filesystem API（用于 APK 环境）
let Filesystem;
let Directory;
try {
  if (typeof window !== 'undefined' && window.Capacitor) {
    Filesystem = window.Capacitor.Plugins.Filesystem;
    Directory = window.Capacitor.Plugins.FilesystemDirectory;
  }
} catch (e) {
  console.warn('Capacitor Filesystem 未加载:', e);
}

// 通用APK环境检测函数
const isAndroidApp = () => {
  return typeof window !== 'undefined' && 
         window.Capacitor && 
         /android/i.test(navigator.userAgent);
};

---

## [2026-04-03] - 导出配置功能修复（支持阵容助手数据）

### 文件：`src/views/TokenImport/index.vue`

#### 修改类型：功能修复

- **导出配置功能增强**：
  - 增加对 `saved_lineups_${tokenId}` 格式阵容数据的读取
  - 将所有账号的阵容数据存储在导出文件的 `lineupsByToken` 字段中
  - 保持对旧格式 `saved_lineups` 的向后兼容性

- **导入配置功能增强**：
  - 增加对 `lineupsByToken` 字段的处理
  - 为每个账号创建对应的 `saved_lineups_${tokenId}` key
  - 恢复各个账号的阵容数据

- **技术实现**：
  - 扫描所有 localStorage key，识别 `saved_lineups_` 前缀的键
  - 解析并整合各个账号的阵容数据
  - 在导入时根据 tokenId 恢复阵容数据

- **兼容性**：
  - 采用向后兼容的方式，不会影响现有的导出配置功能
  - 手机上的导出配置功能仍然可以正常使用

**关键代码修改：**

1. **导出功能增强**：
```javascript
// 读取所有账号的阵容数据
let lineupsData = null;
let lineupsByToken = {};
try {
  // 读取旧格式的阵容数据（向后兼容）
  const saved = localStorage.getItem("saved_lineups");
  if (saved) {
    lineupsData = JSON.parse(saved);
  }
  
  // 读取所有账号的阵容数据（saved_lineups_${tokenId} 格式）
  for (let i = 0; i < localStorage.length; i++) {
    const key = localStorage.key(i);
    if (key && key.startsWith('saved_lineups_')) {
      try {
        const tokenId = key.replace('saved_lineups_', '');
        const savedLineups = localStorage.getItem(key);
        if (savedLineups) {
          lineupsByToken[tokenId] = JSON.parse(savedLineups);
        }
      } catch (e) {
        console.warn(`读取账号 ${key} 的阵容数据失败:`, e);
      }
    }
  }
} catch (e) {
  console.warn('获取阵容数据失败', e);
}

// 在导出数据中添加 lineupsByToken
const exportData = {
  // 其他字段...
  lineupsData: lineupsData,
  lineupsByToken: lineupsByToken,
  // 其他字段...
};
```

2. **导入功能增强**：
```javascript
// 导入账号阵容数据
if (importData.lineupsByToken) {
  try {
    let importedLineupsCount = 0;
    Object.entries(importData.lineupsByToken).forEach(([tokenId, lineups]) => {
      try {
        localStorage.setItem(`saved_lineups_${tokenId}`, JSON.stringify(lineups));
        importedLineupsCount++;
      } catch (e) {
        console.error(`导入账号 ${tokenId} 的阵容数据失败:`, e);
      }
    });
    if (importedLineupsCount > 0) {
      message.info(`账号阵容数据已导入: ${importedLineupsCount} 个账号`);
    }
  } catch (e) {
    console.error('导入账号阵容数据失败:', e);
  }
}
```
