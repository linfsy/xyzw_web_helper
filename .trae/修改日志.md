---

## [2026-04-01] - 优化导出配置显示效果与备份版本一致

### 文件：`src/views/BatchDailyTasks.vue`

#### 修改类型：优化改进

- **优化导出配置函数**：
  - 调整数据获取方式，直接从 tokenStore 获取 tokens 数据
  - 使用 scheduledTaskStore.scheduledTasks 获取定时任务
  - 从 indexedDB 解构获取 waitForReady、getAllKeys、getArrayBuffer 函数
  - 保持与备份版本相同的代码结构和显示逻辑
  - 保留阵容数据导出功能（版本 1.3）

- **显示效果优化**：
  - 导出成功提示信息与备份版本一致
  - 权限检查逻辑与备份版本相同
  - 文件保存路径与备份版本一致（/storage/emulated/0/Documents/）

- **功能保留**：
  - 阵容数据导出功能完整保留
  - 定时任务导出功能正常
  - BIN 文件导出功能正常
  - 所有配置项导出完整

**关键代码修改：**

```javascript
// 优化后的导出配置函数
const exportConfig = async () => {
  try {
    const loadingMsg = message.loading('正在导出配置，读取BIN文件中...', { duration: 0 });
    
    // 从 indexedDB 解构获取函数（与备份版本一致）
    const { waitForReady, getAllKeys, getArrayBuffer } = indexedDB;
    const ready = await waitForReady(3000);
    
    // 直接从 store 获取数据（优化点）
    const tokensData = tokenStore.gameTokens;
    const validTokenIds = new Set(tokensData.map((t) => t.id));
    
    // 从 scheduledTaskStore 获取定时任务
    const filteredScheduledTasks = scheduledTaskStore.scheduledTasks
      .map((task) => ({
        ...task,
        selectedTokens: task.selectedTokens?.filter((tokenId) =>
          validTokenIds.has(tokenId),
        ) || [],
      }))
      .filter((task) => task.selectedTokens.length > 0);
    
    // ... 其余逻辑与备份版本一致
  }
};

// saveFileByDownload 函数移到外部（与备份版本一致）
function saveFileByDownload(jsonString, fileName) {
  // ... 实现与备份版本相同
}
```

---

## [2026-04-01] - 完善导出配置导入配置功能

### 文件：`src/views/TokenImport/index.vue`

#### 修改类型：功能完善

- **导出配置功能**：
  - 版本升级到 1.3
  - 新增阵容数据 (`savedLineups`) 导出
  - 优化导出进度提示
  - 完善导出成功消息，显示阵容数据数量

- **导入配置功能**：
  - 支持 Base64 编码的配置文件
  - 新增阵容数据导入
  - 优化定时任务导入，更新到 Pinia store
  - 完善导入进度提示
  - 添加阵容数据导入成功消息

- **功能验证**：
  - 导出配置包含阵容数据 ✅
  - 导入配置恢复阵容数据 ✅
  - 定时任务导入后显示在列表中 ✅
  - Base64 编码配置文件导入 ✅
  - 导入完成后自动刷新页面 ✅

**关键代码修改：**

1. **导出配置**：
```javascript
// 导出阵容数据
const savedLineups = {};
tokens.forEach((token) => {
  const key = `saved_lineups_${token.id}`;
  const data = localStorage.getItem(key);
  if (data) {
    try {
      savedLineups[token.id] = JSON.parse(data);
    } catch (e) {
      console.warn(`解析阵容数据失败 (${token.id}):`, e);
    }
  }
});

// 导出数据结构
const exportData = {
  version: "1.3",
  exportTime: new Date().toISOString(),
  tokens: tokens.map((t) => ({ /* ... */ })),
  scheduledTasks: scheduledTasks,
  batchSettings: batchSettings,
  taskTemplates: taskTemplates,
  tokenSortConfig: tokenSortConfig,
  tokenSettings: tokenSettings,
  tokenGroups: tokenGroups,
  binFiles: binFiles,
  savedLineups: savedLineups,  // 新增
};

// 导出成功消息
const lineupsCount = Object.keys(exportData.savedLineups || {}).length;
message.success(`导出成功: ${exportData.tokens.length} 个账号, ${exportData.scheduledTasks?.length || 0} 个定时任务, ${Object.keys(exportData.binFiles || {}).length} 个BIN文件${lineupsCount > 0 ? `, ${lineupsCount} 个账号的阵容` : ''}`);
```

2. **导入配置**：
```javascript
// Base64 解码
const decodeBase64 = (str) => {
  try {
    str = str.replace(/[\s\n\r]/g, '');
    try {
      return decodeURIComponent(escape(atob(str)));
    } catch (e) {
      const binary = atob(str);
      const bytes = new Uint8Array(binary.length);
      for (let i = 0; i < binary.length; i++) {
        bytes[i] = binary.charCodeAt(i);
      }
      return new TextDecoder().decode(bytes);
    }
  } catch (e) {
    throw new Error('Base64解码失败: ' + e.message);
  }
};

// 阵容数据导入
if (importData.savedLineups && typeof importData.savedLineups === 'object') {
  let lineupsCount = 0;
  try {
    Object.entries(importData.savedLineups).forEach(([tokenId, lineups]) => {
      try {
        const key = `saved_lineups_${tokenId}`;
        localStorage.setItem(key, JSON.stringify(lineups));
        lineupsCount++;
      } catch (e) {
        console.warn(`导入阵容数据失败 (${tokenId}):`, e);
      }
    });
    if (lineupsCount > 0) {
      message.info(`阵容数据导入完成: ${lineupsCount} 个账号`);
    }
  } catch (e) {
    console.error('导入阵容数据失败:', e);
  }
}

// 定时任务导入更新到 Pinia store
if (scheduledTaskStore) {
  // 清空现有的任务
  while (scheduledTaskStore.scheduledTasks.length > 0) {
    scheduledTaskStore.removeTask(scheduledTaskStore.scheduledTasks[0].id);
  }
  // 添加新的任务
  importData.scheduledTasks.forEach(task => {
    scheduledTaskStore.addTask({
      name: task.name,
      taskName: task.taskName,
      runType: task.runType,
      runTime: task.runTime,
      cronExpression: task.cronExpression,
      selectedTokens: task.selectedTokens,
      selectedTasks: task.selectedTasks,
      enableBatchExecution: task.enableBatchExecution,
      batchSize: task.batchSize,
      batchDelay: task.batchDelay
    });
  });
  // 重新启动调度器，确保状态同步
  scheduledTaskStore.startScheduler();
  message.info('定时任务已同步到状态管理');
}
```

---

## [2026-04-01] - 修复批量任务核心功能并完善阵容助手

### 文件：`src/views/BatchDailyTasks.vue`, `src/utils/batch/tasksTower.js`, `src/utils/batch/tasksArena.js`, `src/utils/batch/constants.js`, `src/views/TokenImport/index.vue`

#### 修改类型：功能修复与完善

- **功能修复**：
  - 修复 `wrappedBatchLegacyGiftSendEnhanced is not defined` 错误
  - 添加 `getTokenSettings` 函数到 `tasksTower.js` 和 `tasksArena.js`
  - 添加错误处理（1500020能量不足、1500040上座奖励未领）
  - 添加 WebSocket 重连逻辑
  
- **功能完善**：
  - 完善阵容助手导出导入功能
  - 阵容数据随配置文件一起导出导入
  - 添加阵容数据导入成功消息提示
  
- **核心功能验证**：
  - Token管理页面导入导出配置 ✅
  - 批量任务分批执行 ✅
  - 阵容切换统一命令 `presetteam_saveteam` ✅
  - 怪异塔单独阵容设置 ✅
  - 暂停时间检测功能 ✅
  - 积攒任务恢复功能 ✅
  - 账号列表折叠功能 ✅
  - 任务功能按键折叠功能 ✅
  - 任务持久化功能 ✅
  - 黑市周购买功能 ✅
  - 智能发车功能 ✅
  - 定时任务调度器 ✅

**关键代码修改：**

1. **BatchDailyTasks.vue**：
```javascript
// 修复缺失的包装函数
const wrappedBatchLegacyGiftSendEnhanced = () => executeInBatches(batchLegacyGiftSendEnhanced, '批量功法残卷赠送', 'batchLegacyGiftSendEnhanced');

// 阵容数据导入导出
const savedLineups = {};
tokens.value.forEach((token) => {
  const key = `saved_lineups_${token.id}`;
  const data = localStorage.getItem(key);
  if (data) {
    savedLineups[token.id] = JSON.parse(data);
  }
});
```

2. **tasksTower.js**：
```javascript
// 添加 getTokenSettings 函数
const getTokenSettings = (tokenId) => {
  const defaultSettings = {
    arenaFormation: 1,
    towerFormation: 1,
    weirdTowerFormation: 1,
    bossFormation: 1,
    taskCompleteFormation: 1,
    // ...
  };
  // ...
};

// 添加错误处理
if (err.message && err.message.includes("1500020")) {
  // 能量不足处理
}
if (err.message && err.message.includes("1500040")) {
  // 上座奖励未领处理
}
```

3. **tasksArena.js**：
```javascript
// 添加 getTokenSettings 函数
const getTokenSettings = (tokenId) => {
  // ...
};
```

---

## [2026-04-01] - 恢复批量任务核心功能

### 文件：`src/views/BatchDailyTasks.vue`, `src/utils/batch/tasksTower.js`, `src/utils/batch/tasksArena.js`, `src/utils/batch/constants.js`

#### 修改类型：功能恢复

- **功能恢复**：恢复批量任务核心功能
  - 添加 `taskFunctionMap` 和 `getTaskFunction` 函数
  - 添加 `weirdTowerFormation` 怪异塔阵容设置
  - 添加 `taskCompleteFormation` 任务完成后阵容设置
  - 更新 `climbTower` 和 `climbWeirdTower` 使用独立阵容
  - 更新 `batcharenafight` 支持任务完成后阵容切换
  - 更新 `defaultSettings` 和 `defaultTemplate`

**关键代码修改：**

1. **BatchDailyTasks.vue**：
```javascript
// 添加 taskFunctionMap 和 getTaskFunction
const taskFunctionMap = {
  claimHangUpRewards: wrappedClaimHangUpRewards,
  batchAddHangUpTime: wrappedBatchAddHangUpTime,
  // ... 其他任务函数
};

const getTaskFunction = (taskName) => {
  return taskFunctionMap[taskName] || null;
};

// 添加阵容设置
const currentSettings = reactive({
  arenaFormation: 1,
  towerFormation: 1,
  weirdTowerFormation: 1,  // 新增
  bossFormation: 1,
  taskCompleteFormation: 1,  // 新增
  // ... 其他设置
});
```

2. **tasksTower.js**：
```javascript
// climbWeirdTower 使用 weirdTowerFormation
if (currentFormation === tokenSettings.weirdTowerFormation) {
  // ...
}

// 任务完成后切换阵容
if (tokenSettings.taskCompleteFormation && tokenSettings.taskCompleteFormation !== tokenSettings.weirdTowerFormation) {
  await tokenStore.sendMessageWithPromise(
    tokenId,
    "presetteam_saveteam",
    { teamId: tokenSettings.taskCompleteFormation },
    5000,
  );
}
```

3. **tasksArena.js**：
```javascript
// 竞技场任务完成后切换阵容
if (tokenSettings.taskCompleteFormation && tokenSettings.taskCompleteFormation !== tokenSettings.arenaFormation) {
  await tokenStore.sendMessageWithPromise(
    tokenId,
    "presetteam_saveteam",
    { teamId: tokenSettings.taskCompleteFormation },
    5000,
  );
}
```

4. **constants.js**：
```javascript
export const defaultSettings = {
  arenaFormation: 1,
  towerFormation: 1,
  weirdTowerFormation: 1,  // 新增
  bossFormation: 1,
  taskCompleteFormation: 1,  // 新增
  // ...
};

export const defaultTemplate = {
  arenaFormation: 1,
  towerFormation: 1,
  weirdTowerFormation: 1,  // 新增
  bossFormation: 1,
  taskCompleteFormation: 1,  // 新增
  // ...
};
```
