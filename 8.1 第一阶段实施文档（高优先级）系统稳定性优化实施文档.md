# 第一阶段实施文档（高优先级）

## 文档说明

本文档详细描述系统稳定性优化第一阶段的实施方案，包含4个高优先级功能点的具体实现细节、代码示例、测试方法和风险评估。

**文档版本**：v1.0  
**创建日期**：2026-03-12  
**适用项目**：xyzw_web_helper  
**实施时间**：约45分钟  

---

## 目录

1. [阶段概述](#1-阶段概述)
2. [功能模块清单](#2-功能模块清单)
3. [详细实施方案](#3-详细实施方案)
4. [测试计划](#4-测试计划)
5. [风险评估与应对](#5-风险评估与应对)
6. [实施时间表](#6-实施时间表)
7. [回滚方案](#7-回滚方案)

---

## 1. 阶段概述

### 1.1 阶段目标
实现4个高优先级功能，解决系统核心稳定性问题，确保积攒列表和暂停时间功能正常运行。

### 1.2 功能范围
- **智能任务去重**：避免重复任务被添加到队列
- **安全队列管理**：确保成功的任务被移除，失败的任务保留
- **错误处理增强**：防止数据丢失，提升系统稳定性
- **实时状态检测**：确保暂停时间被及时检测和处理

### 1.3 预期效果
- 消除任务重复添加的问题
- 确保队列管理逻辑正确
- 提升系统错误处理能力
- 确保暂停时间检测及时响应

---

## 2. 功能模块清单

| 序号 | 功能名称 | 优先级 | 复杂度 | 预计时间 | 涉及文件 |
|------|----------|--------|--------|----------|----------|
| 1.1 | 智能任务去重 | 高 | 中等 | 15分钟 | batchTaskStore.ts, BatchDailyTasks.vue |
| 1.2 | 安全队列管理 | 高 | 中等 | 15分钟 | BatchDailyTasks.vue |
| 1.3 | 错误处理增强 | 高 | 简单 | 10分钟 | BatchDailyTasks.vue |
| 2.1 | 实时状态检测 | 高 | 中等 | 15分钟 | BatchDailyTasks.vue |

---

## 3. 详细实施方案

### 3.1 智能任务去重（1.1）

**功能描述**：
同时检查任务名称、类型、账号列表和任务类型，避免重复任务被添加到队列中。

**实现逻辑**：
1. 检查任务名称是否相同
2. 检查运行类型是否相同
3. 检查选中的账号列表是否相同（长度和每个元素）
4. 检查选中的任务类型是否相同（排序后比较）

**涉及文件**：
- `src/stores/batchTaskStore.ts` - `addToTaskQueue` 函数
- `src/views/BatchDailyTasks.vue` - 多处任务添加逻辑

**代码实现**：

**文件1：src/stores/batchTaskStore.ts**
```typescript
function addToTaskQueue(task: {
  id?: number;
  name?: string;
  selectedTokens?: string[];
  selectedTasks?: string[];
  runType?: string;
}) {
  // 同时检查任务名称、运行类型、账号列表和任务类型
  const isTaskExists = taskQueue.value.some(existingTask => {
    // 检查任务名称和类型
    if (existingTask.name === task.name && existingTask.runType === task.runType) {
      // 检查选中的账号是否相同
      const existingTokens = existingTask.selectedTokens || [];
      const newTokens = task.selectedTokens || [];
      const sameTokens = existingTokens.length === newTokens.length && 
             existingTokens.every(tokenId => newTokens.includes(tokenId));
      
      // 检查选中的任务类型是否相同
      const existingTasks = existingTask.selectedTasks || [];
      const newTasks = task.selectedTasks || [];
      const sameTasks = JSON.stringify(existingTasks.sort()) === JSON.stringify(newTasks.sort());
      
      // 只有名称、类型、账号和任务类型都相同才认为是重复任务
      return sameTokens && sameTasks;
    }
    return false;
  });
  
  if (!isTaskExists) {
    taskQueue.value.push(task);
    console.log('[BatchTask] 任务已加入队列:', task.name);
  } else {
    console.log('[BatchTask] 任务已在队列中，跳过重复添加:', task.name);
  }
}
```

**文件2：src/views/BatchDailyTasks.vue（定时任务调度器）**
```javascript
// 检查任务是否已经在队列中（同时检查任务名称、任务类型和账号列表）
const existingTaskIndex = batchTaskStore.taskQueue.findIndex(t => {
  const sameName = t.name === task.name;
  const sameTasks = JSON.stringify(t.selectedTasks?.sort()) === JSON.stringify(task.selectedTasks?.sort());
  const sameTokens = JSON.stringify(t.selectedTokens?.sort()) === JSON.stringify(task.selectedTokens?.sort());
  return sameName && sameTasks && sameTokens;
});
```

**文件3：src/views/BatchDailyTasks.vue（executeInBatches函数）**
```javascript
// 检查任务是否已经在积攒队列中（避免重复添加）
// 同时检查任务名称、任务类型和账号列表
const existingTask = batchTaskStore.taskQueue.find(t => {
  const sameName = t.name === taskName;
  const sameTasks = JSON.stringify(t.selectedTasks?.sort()) === JSON.stringify([taskFunctionName || taskName].sort());
  const sameTokens = JSON.stringify(t.selectedTokens?.sort()) === JSON.stringify(remainingTokens.sort());
  return sameName && sameTasks && sameTokens;
});
```

**测试方法**：
1. 添加一个任务到积攒队列
2. 尝试添加相同名称、相同类型、相同账号的任务
3. 验证任务未被重复添加
4. 尝试添加相同名称但不同类型或不同账号的任务
5. 验证任务被正确添加

**注意事项**：
- 确保所有比较都使用排序后的数组，避免顺序不同导致的误判
- 处理空数组的情况
- 添加详细的日志记录

---

### 3.2 安全队列管理（1.2）

**功能描述**：
成功的任务逐个移除，失败的任务保留在队列中，避免统一清空队列的操作。

**实现逻辑**：
1. 任务执行成功后，立即从队列中移除该任务
2. 任务执行失败后，保留在队列中
3. 保存更新后的队列到本地存储
4. 添加日志记录

**涉及文件**：
- `src/views/BatchDailyTasks.vue` - `executeQueuedTasks` 函数

**代码实现**：

**文件：src/views/BatchDailyTasks.vue**
```javascript
// 在任务执行成功后的处理逻辑
try {
  if (hasDailyTask) {
    selectedTokens.value = currentTaskTokens;
    await startBatch(true); // 传入true表示从积攒队列执行
  }
  
  for (const subTaskName of otherTasks) {
    const taskFunction = getOriginalTaskFunction(subTaskName);
    if (typeof taskFunction === 'function') {
      selectedTokens.value = currentTaskTokens;
      await executeInBatches(taskFunction, taskName, subTaskName, true);
    }
  }
  
  // 重要：任务执行成功后，立即从积攒队列中移除
  // 避免页面刷新后重复执行已完成的任务
  const taskIndex = batchTaskStore.taskQueue.findIndex(t => t.id === task.id);
  if (taskIndex > -1) {
    batchTaskStore.taskQueue.splice(taskIndex, 1);
    // 保存更新后的队列到本地存储
    safeLocalStorage.setItem('batch_task_queue', JSON.stringify(batchTaskStore.taskQueue));
    addLog({
      time: new Date().toLocaleTimeString(),
      message: `=== 积攒任务 ${taskName} 执行完成，已从队列中移除 ===`,
      type: "success",
    });
  }
} finally {
  // 清除任务正在执行的标记
  if (task.id) {
    safeLocalStorage.removeItem(`task_executing_${task.id}`);
  }
}
```

**失败任务处理**：
```javascript
// 在循环结束后，保存更新后的队列
try {
  safeLocalStorage.setItem('batch_task_queue', JSON.stringify(batchTaskStore.taskQueue));
} catch (serializationError) {
  console.error('保存任务队列失败:', serializationError);
  addLog({
    time: new Date().toLocaleTimeString(),
    message: `=== 保存积攒队列失败: ${serializationError.message}，保留现有数据 ===`,
    type: "warning",
  });
}

if (failedTasks.length > 0) {
  // 有任务失败，保留失败的任务在队列中
  addLog({
    time: new Date().toLocaleTimeString(),
    message: `=== 执行完成，${failedTasks.length} 个任务执行失败，已保留在队列中 ===`,
    type: "warning",
  });
} else {
  // 所有任务都已处理完成
  addLog({
    time: new Date().toLocaleTimeString(),
    message: `=== 积攒队列任务执行完成 ===`,
    type: "success",
  });
}
```

**测试方法**：
1. 添加多个任务到积攒队列
2. 执行积攒队列，让部分任务成功，部分任务失败
3. 验证成功的任务被移除，失败的任务保留
4. 刷新页面，验证队列状态正确

**注意事项**：
- 确保在任务执行成功后立即移除
- 失败的任务要保留在队列中，便于重试
- 保存队列到本地存储，确保页面刷新后状态不丢失

---

### 3.3 错误处理增强（1.3）

**功能描述**：
数据加载/保存失败时保留现有数据，避免数据丢失。

**实现逻辑**：
1. 加载失败时，保留现有队列数据
2. 保存失败时，保留现有队列数据
3. 添加错误日志，便于排查问题

**涉及文件**：
- `src/views/BatchDailyTasks.vue` - `loadTaskQueueFromStorage` 函数
- `src/views/BatchDailyTasks.vue` - `saveTaskQueueToStorage` 函数

**代码实现**：

**文件1：src/views/BatchDailyTasks.vue（加载队列）**
```javascript
const loadTaskQueueFromStorage = () => {
  // 如果队列已经有数据，先清空（避免热更新时重复添加）
  if (batchTaskStore.taskQueue.length > 0) {
    batchTaskStore.clearTaskQueue();
  }
  
  // 优先从新的键名加载
  const savedQueue = safeLocalStorage.getItem('batch_task_queue');
  if (savedQueue && typeof savedQueue === 'string' && savedQueue !== 'undefined') {
    try {
      const parsedQueue = JSON.parse(savedQueue);
      if (Array.isArray(parsedQueue)) {
        parsedQueue.forEach(task => {
          batchTaskStore.addToTaskQueue(task);
        });
        if (batchTaskStore.taskQueue.length > 0) {
          addLog({
            time: new Date().toLocaleTimeString(),
            message: `=== 从本地存储加载了 ${batchTaskStore.taskQueue.length} 个积攒任务 ===`,
            type: "info",
          });
        }
      }
    } catch (error) {
      console.error('加载任务队列失败:', error);
      // 重要修复：加载失败时不清空队列，保留现有队列数据
      // 避免数据丢失，只在明确需要清空时才清空
      addLog({
        time: new Date().toLocaleTimeString(),
        message: `=== 加载积攒队列失败: ${error.message}，保留现有队列 ===`,
        type: "warning",
      });
    }
  }
  
  // 清理旧的键名
  safeLocalStorage.removeItem('taskQueue');
};
```

**文件2：src/views/BatchDailyTasks.vue（保存队列）**
```javascript
const saveTaskQueueToStorage = () => {
  try {
    // 验证任务队列数据结构，确保可以安全序列化
    const queue = batchTaskStore.taskQueue || [];
    if (!Array.isArray(queue)) {
      console.error('任务队列不是数组:', queue);
      return;
    }
    
    // 限制队列长度，避免存储过大的数据
    const limitedQueue = queue.slice(0, 50); // 最多保存50个任务
    
    // 序列化前进行数据清理，移除可能导致循环引用的属性
    const sanitizedQueue = limitedQueue.map(task => {
      const sanitizedTask = { ...task };
      // 移除可能导致序列化问题的属性
      if (sanitizedTask.params) {
        // 只保留必要的参数
        sanitizedTask.params = {
          taskName: sanitizedTask.params.taskName,
          subTaskName: sanitizedTask.params.subTaskName,
          // 其他必要参数...
        };
      }
      return sanitizedTask;
    });
    
    const dataToSave = JSON.stringify(sanitizedQueue);
    safeLocalStorage.setItem('batch_task_queue', dataToSave);
  } catch (error) {
    console.error('保存任务队列失败:', error);
    // 重要修复：保存失败时不清空存储，保留现有数据
    // 避免数据丢失，只在明确需要清空时才清空
    addLog({
      time: new Date().toLocaleTimeString(),
      message: `=== 保存积攒队列失败: ${error.message}，保留现有数据 ===`,
      type: "warning",
    });
  }
};
```

**测试方法**：
1. 模拟加载失败（如损坏的JSON数据）
2. 验证现有队列数据被保留
3. 模拟保存失败（如存储空间不足）
4. 验证现有存储数据被保留

**注意事项**：
- 错误处理要完善，不能导致数据丢失
- 添加详细的错误日志
- 考虑各种异常情况

---

### 3.4 实时状态检测（2.1）

**功能描述**：
定时检查暂停状态，确保及时响应，批量执行等待期间也检查暂停状态。

**实现逻辑**：
1. 每分钟检查一次暂停状态
2. 在批处理等待期间定期检查暂停状态
3. 检测到暂停时，将任务加入积攒队列

**涉及文件**：
- `src/views/BatchDailyTasks.vue` - `updatePauseStatus` 函数
- `src/views/BatchDailyTasks.vue` - 批处理等待逻辑

**代码实现**：

**文件1：src/views/BatchDailyTasks.vue（暂停状态检测）**
```javascript
// 暂停时间检测
const isPauseTime = ref({
  paused: false,
  reason: '',
  nextResumeTime: null
});

const updatePauseStatus = () => {
  const now = new Date();
  const currentHour = now.getHours();
  const currentMinute = now.getMinutes();
  const currentDay = now.getDay(); // 0=周日, 6=周六
  
  // 检查抢符时间（20:00-20:30）
  if (currentHour === 20 && currentMinute >= 0 && currentMinute <= 30) {
    isPauseTime.value = {
      paused: true,
      reason: '抢符时间',
      nextResumeTime: new Date(now.getTime() + (30 - currentMinute) * 60 * 1000)
    };
    return;
  }
  
  // 检查周六盐场（周六20:00-21:00）
  if (currentDay === 6 && currentHour === 20) {
    isPauseTime.value = {
      paused: true,
      reason: '周六盐场',
      nextResumeTime: new Date(now.getTime() + (60 - currentMinute) * 60 * 1000)
    };
    return;
  }
  
  // 检查周日盐场（周日20:00-21:00）
  if (currentDay === 0 && currentHour === 20) {
    isPauseTime.value = {
      paused: true,
      reason: '周日盐场',
      nextResumeTime: new Date(now.getTime() + (60 - currentMinute) * 60 * 1000)
    };
    return;
  }
  
  // 检查游戏更新时间（根据配置）
  const updateTime = getGameUpdateTime(); // 从配置中获取
  if (updateTime && isDuringUpdate(now, updateTime)) {
    isPauseTime.value = {
      paused: true,
      reason: '游戏更新',
      nextResumeTime: getUpdateEndTime(updateTime)
    };
    return;
  }
  
  // 不在暂停时间
  isPauseTime.value = {
    paused: false,
    reason: '',
    nextResumeTime: null
  };
};

// 每分钟检查一次暂停状态
setInterval(updatePauseStatus, 60 * 1000);
```

**文件2：src/views/BatchDailyTasks.vue（批处理等待期间检测）**
```javascript
// 批量执行等待期间检查暂停状态
if (batchIndex < totalBatches - 1 && !batchTaskStore.shouldStop.value) {
  addLog({
    time: new Date().toLocaleTimeString(),
    message: `=== 第 ${batchIndex + 1} 批执行完成，等待 ${batchDelay / 1000} 秒后执行下一批 ===`,
    type: "info",
  });

  let remainingSeconds = batchDelay / 1000;
  
  while (remainingSeconds > 0 && !batchTaskStore.shouldStop.value) {
    // 重要：确保在等待期间 isRunning 保持为 true
    if (!batchTaskStore.isRunning) {
      batchTaskStore.startTask();
    }
    
    // 检查暂停状态
    if (isPauseTime.value.paused) {
      addLog({
        time: new Date().toLocaleTimeString(),
        message: `=== 批量任务被暂停: 当前处于${isPauseTime.value.reason}，已加入积攒队列 ===`,
        type: "info",
      });
      
      // 将剩余任务加入积攒队列
      const remainingTokens = allTokens.slice((batchIndex + 1) * batchSize);
      batchTaskStore.addToTaskQueue({
        name: task.name,
        selectedTokens: remainingTokens,
        selectedTasks: taskTasksList,
        runType: task.runType,
        id: task.id
      });
      
      selectedTokens.value = [...originalSelectedTokens];
      return;
    }
    
    await new Promise(resolve => setTimeout(resolve, 1000));
    remainingSeconds--;
  }
}
```

**测试方法**：
1. 设置系统时间在暂停时间段内
2. 启动批量任务
3. 验证任务被暂停并加入积攒队列
4. 等待暂停时间结束
5. 验证任务自动恢复执行

**注意事项**：
- 暂停检测要实时，不能延迟太久
- 批处理等待期间也要检测暂停状态
- 暂停时要保存任务状态，便于恢复

---

## 4. 测试计划

### 4.1 功能测试

| 测试项 | 测试步骤 | 预期结果 |
|--------|----------|----------|
| 任务去重 | 添加相同任务两次 | 第二次添加被跳过 |
| 队列管理 | 执行积攒队列，部分成功部分失败 | 成功的移除，失败的保留 |
| 错误处理 | 模拟加载/保存失败 | 现有数据被保留 |
| 实时检测 | 设置暂停时间，启动任务 | 任务被暂停并加入积攒队列 |

### 4.2 边界测试

| 测试项 | 测试步骤 | 预期结果 |
|--------|----------|----------|
| 网络异常 | 网络中断后恢复 | 任务可以继续执行 |
| 存储异常 | 存储空间不足 | 系统能够正常运行 |
| 大量任务 | 同时执行多个定时任务 | 系统稳定 |

### 4.3 压力测试

| 测试项 | 测试步骤 | 预期结果 |
|--------|----------|----------|
| 并发测试 | 同时执行10个以上定时任务 | 系统稳定 |
| 批量测试 | 50个账号的批量执行 | 系统稳定 |
| 暂停测试 | 频繁的暂停/恢复操作 | 系统稳定 |

---

## 5. 风险评估与应对

### 5.1 风险识别

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 任务去重过于严格 | 高 | 合法任务被跳过 | 充分测试去重逻辑，确保不误判 |
| 暂停检测过于频繁 | 中 | 影响性能 | 优化检测频率，使用合理的间隔 |
| 批处理暂停导致中断 | 中 | 任务执行不完整 | 完善暂停处理逻辑，确保状态保存 |
| 错误处理不当 | 低 | 数据丢失 | 充分测试错误处理逻辑 |

### 5.2 风险应对策略

- **代码层面**：每个修改都添加详细的注释，使用 try-catch 保护所有关键操作
- **测试层面**：每个功能修改后都进行单独测试，进行完整的回归测试
- **部署层面**：创建完整备份，便于回滚，监控日志，及时发现问题

---

## 6. 实施时间表

**总时长：约45分钟**

| 任务 | 时间 | 内容 |
|------|------|------|
| 准备 | 5分钟 | 创建备份、熟悉代码 |
| 智能任务去重 | 15分钟 | 修改 addToTaskQueue 函数和多处任务添加逻辑 |
| 安全队列管理 | 15分钟 | 修改 executeQueuedTasks 函数 |
| 错误处理增强 | 10分钟 | 修改 loadTaskQueueFromStorage 和 saveTaskQueueToStorage 函数 |
| 实时状态检测 | 15分钟 | 修改 updatePauseStatus 函数和批处理等待逻辑 |
| 测试 | 10分钟 | 功能测试、边界测试 |
| 构建 | 5分钟 | 构建APK、创建备份 |

---

## 7. 回滚方案

### 7.1 回滚条件

- 修改后出现严重BUG，影响系统正常使用
- 性能下降明显，用户体验变差
- 数据丢失或损坏

### 7.2 回滚步骤

1. **停止当前操作**
   - 停止所有正在执行的任务
   - 保存当前状态

2. **恢复备份**
   - 从备份目录恢复文件
   - 或使用 git 回滚到之前的版本

3. **验证恢复**
   - 检查文件是否恢复正确
   - 测试基本功能是否正常

4. **重新部署**
   - 构建APK
   - 部署到生产环境

### 7.3 预防措施

- 创建完整备份，便于回滚
- 使用版本控制，便于回滚
- 保留修改前的代码注释
- 记录所有修改，便于排查问题

---

## 8. 总结

### 8.1 阶段收益

- **消除核心BUG**：修复任务重复添加、队列管理错误等核心问题
- **提升稳定性**：增强错误处理能力，确保数据安全
- **优化用户体验**：暂停时间检测及时响应，提升用户体验
- **为后续优化奠定基础**：为第二、三阶段的优化做好准备

### 8.2 关键成功因素

- **充分测试**：每个功能都要经过充分测试
- **逐步实施**：按顺序实施，降低风险
- **及时回滚**：发现问题及时回滚
- **持续监控**：部署后持续监控系统运行

### 8.3 后续工作

- **第二阶段实施**：实施中优先级的3个功能
- **第三阶段实施**：实施低优先级的8个功能
- **持续优化**：根据用户反馈持续优化

---

**文档结束**

**注意**：本文档为第一阶段实施文档，请在实施前仔细阅读，确保理解每个功能点的实现细节。