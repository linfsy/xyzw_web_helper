# 阵容助手组件完整文档

## 1. 组件概述

阵容助手是一个功能强大的游戏阵容管理工具，允许用户保存、切换和管理游戏中的英雄阵容。该组件基于Vue 3和Naive UI开发，提供了直观的用户界面和丰富的功能。

## 2. 功能特性

- **阵容数据刷新**：实时获取当前阵容信息和英雄数据
- **阵容槽位管理**：支持多个阵容槽位的切换和管理
- **英雄上阵/更换**：可以轻松上阵新英雄或更换现有英雄
- **阵容保存**：将当前阵容保存到本地存储
- **阵容应用**：快速应用已保存的阵容配置
- **装备洗练查看**：查看英雄装备的洗练属性
- **拖拽调整站位**：通过拖拽方式调整英雄在阵容中的位置
- **英雄筛选**：按品质和国家筛选英雄
- **阵容重命名和删除**：管理已保存的阵容

## 3. 核心代码结构

### 3.1 组件模板结构

```vue
<template>
  <MyCard class="lineup-saver" :statusClass="{ active: state.isRunning }">
    <template #icon>
      <img src="/icons/Ob7pyorzmHiJcbab2c25af264d0758b527bc1b61cc3b.png" alt="阵容图标" />
    </template>
    <template #title>
      <h3>阵容助手</h3>
      <p>保存阵容、快速切换</p>
    </template>
    <!-- 工具栏 -->
    <div class="toolbar">
      <!-- 刷新数据、保存阵容、上阵英雄、查看已保存阵容按钮 -->
    </div>
    <!-- 阵容槽位选择 -->
    <div class="quick-switch-section">
      <!-- 阵容槽位按钮 -->
    </div>
    <!-- 编辑阵容区域 -->
    <div class="current-team-section">
      <!-- 英雄列表，支持拖拽调整位置 -->
    </div>
    <!-- 已保存阵容模态框 -->
    <n-modal v-model:show="savedLineupsModalVisible">
      <!-- 已保存阵容列表 -->
    </n-modal>
    <!-- 装备洗练模态框 -->
    <n-modal v-model:show="refineModalVisible">
      <!-- 装备洗练信息 -->
    </n-modal>
    <!-- 英雄更换/上阵模态框 -->
    <n-modal v-model:show="exchangeModalVisible">
      <!-- 英雄选择界面 -->
    </n-modal>
  </MyCard>
</template>
```

### 3.2 核心脚本逻辑

```javascript
<script setup>
import { ref, computed, onMounted, watch, h } from "vue";
import { useMessage, useDialog, NInput } from "naive-ui";
import { useTokenStore } from "@/stores/tokenStore";
import MyCard from "../Common/MyCard.vue";
import { HERO_DICT, FishMap } from "@/utils/HeroList.js";

const tokenStore = useTokenStore();
const message = useMessage();
const dialog = useDialog();

// 响应式状态
const loading = ref(false);
const currentTeamId = ref(1);
const availableTeams = ref([1, 2, 3, 4, 5, 6]);
const currentTeamInfo = ref(null);
const savedLineups = ref([]);
const allHeroesData = ref({});
const roleHeroesData = ref({});
const editingTeamHeroes = ref({});

// 模态框状态
const savedLineupsModalVisible = ref(false);
const refineModalVisible = ref(false);
const exchangeModalVisible = ref(false);

// 核心方法
const refreshTeamInfo = async () => {
  // 获取阵容和英雄数据
};

const saveCurrentLineup = () => {
  // 保存当前阵容到本地存储
};

const applyLineup = debounce(async (lineup) => {
  // 应用已保存的阵容
}, 1000);

const switchTeam = async (teamId) => {
  // 切换阵容槽位
};

// 生命周期钩子
onMounted(() => {
  loadSavedLineups();
  // 检查WebSocket连接并刷新数据
});

// 监听器
watch(() => tokenStore.selectedToken, (newToken) => {
  // 当选中的Token变化时，重新加载数据
});
</script>
```

### 3.3 样式结构

```scss
<style scoped lang="scss">
.lineup-saver {
  min-height: 300px;
}

.lineup-container {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md);
}

.toolbar {
  display: flex;
  gap: var(--spacing-sm);
  align-items: center;
}

.heroes-grid {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-sm);
}

.hero-item {
  display: flex;
  align-items: center;
  gap: var(--spacing-xs);
  background: var(--bg-primary);
  border-radius: var(--border-radius-small);
  padding: var(--spacing-xs) var(--spacing-sm);
  min-width: 100px;
  transition: all 0.2s;
  cursor: grab;
  border: 2px solid transparent;
  
  // 拖拽状态样式
  &.dragging {
    opacity: 0.5;
    cursor: grabbing;
  }
  
  &.drag-over {
    border-color: var(--primary-color);
    background: var(--primary-color-light);
  }
}

/* 其他样式定义 */
</style>
```

## 4. 依赖项

| 依赖项 | 用途 | 来源 |
|--------|------|------|
| Vue 3 | 前端框架 | 项目依赖 |
| Naive UI | UI组件库 | 项目依赖 |
| Pinia | 状态管理 | 项目依赖 |
| tokenStore | WebSocket通信 | @/stores/tokenStore |
| MyCard | 卡片组件 | ../Common/MyCard.vue |
| HERO_DICT, FishMap | 英雄和鱼灵数据 | @/utils/HeroList.js |

## 5. 关键功能实现

### 5.1 数据刷新

```javascript
const refreshTeamInfo = async (retryCount = 0) => {
  const token = tokenStore.selectedToken;
  if (!token) {
    message.warning("请先选择Token");
    return;
  }

  const tokenId = token.id;
  const status = tokenStore.getWebSocketStatus(tokenId);
  if (status !== "connected") {
    message.error("WebSocket未连接，无法获取数据");
    return;
  }

  loading.value = true;
  try {
    // 分别获取角色信息和阵容信息
    let roleInfo = null;
    let presetTeamResult = null;

    // 尝试获取角色信息
    try {
      roleInfo = await sendMessageWithRetry("role_getroleinfo", {});
      const role = roleInfo?.role || roleInfo;
      roleHeroesData.value = role?.heroes || {};
      allHeroesData.value = role?.heroes || {};
      artifactBooks.value = role?.artifactBooks || {};
      pearlMap.value = role?.pearlMap || {};
    } catch (error) {
      console.warn("获取角色信息失败，继续获取阵容信息:", error.message);
    }

    // 尝试获取阵容信息
    try {
      presetTeamResult = await sendMessageWithRetry("presetteam_getinfo", {});
      presetTeamData.value = presetTeamResult?.presetTeamInfo;

      if (presetTeamData.value) {
        currentTeamId.value = presetTeamData.value.useTeamId || 1;
        const teams2 = presetTeamData.value.presetTeamInfo || {};
        const teamIds2 = Object.keys(teams2)
          .filter((k) => /^\d+$/.test(k))
          .map(Number)
          .sort((a, b) => a - b);
        availableTeams.value = teamIds2.length ? teamIds2 : [1, 2, 3, 4, 5, 6];

        const currentTeam = teams2[currentTeamId.value] || teams2[String(currentTeamId.value)];
        currentTeamInfo.value = currentTeam?.teamInfo || {};
        editingTeamHeroes.value = {};
      }
    } catch (error) {
      // 错误处理和重试逻辑
    }

    message.success("数据已刷新");
  } catch (error) {
    // 错误处理和自动重试
  } finally {
    loading.value = false;
  }
};
```

### 5.2 阵容保存

```javascript
const saveCurrentLineup = () => {
  if (editingHeroes.value.length === 0) {
    message.warning("当前阵容为空，无法保存");
    return;
  }

  const lineupName = `阵容${currentTeamId.value} - ${new Date().toLocaleTimeString()}`;

  const heroesData = editingHeroes.value.map((hero) => {
    return {
      position: hero.position,
      heroId: hero.heroId,
      attachmentUid: hero.attachmentUid || null,
    };
  });

  savedLineups.value.unshift({
    name: lineupName,
    heroes: heroesData,
    teamId: currentTeamId.value,
    savedAt: Date.now(),
    applying: false,
  });

  saveLineupsToStorage();
  message.success(`阵容已保存: ${lineupName}`);
};
```

### 5.3 阵容应用

```javascript
const applyLineup = debounce(async (lineup) => {
  const token = tokenStore.selectedToken;
  if (!token) {
    message.warning("请先选择Token");
    return;
  }

  const tokenId = token.id;
  const status = tokenStore.getWebSocketStatus(tokenId);
  if (status !== "connected") {
    message.error("WebSocket未连接，无法应用阵容");
    return;
  }

  lineup.applying = true;
  state.value.isRunning = true;
  const errors = [];

  try {
    const targetHeroes = [...lineup.heroes];
    
    // 获取最新数据
    let { heroes, teamInfo } = await fetchLatestData();
    let currentHeroes = getTeamHeroes(teamInfo);

    // 处理装备持有者
    const attachmentToHero = {};
    for (const [id, hero] of Object.entries(heroes)) {
      if (hero.attachmentUid && hero.attachmentUid !== -1) {
        attachmentToHero[hero.attachmentUid] = Number(id);
      }
    }

    // 处理装备更换
    for (const targetHero of targetHeroes) {
      if (!targetHero.attachmentUid || targetHero.attachmentUid === -1)
        continue;

      const currentHolderId = attachmentToHero[targetHero.attachmentUid];
      if (currentHolderId && currentHolderId !== targetHero.heroId) {
        // 处理装备更换逻辑
      }
    }

    // 处理下阵不需要的英雄
    for (const hero of currentHeroes) {
      if (!targetHeroIds.has(hero.heroId)) {
        // 处理下阵逻辑
      }
    }

    // 处理位置调整
    const needPositionFix = targetHeroes.some((targetHero) => {
      const currentHero = currentHeroes.find(
        (h) => h.heroId === targetHero.heroId,
      );
      return !currentHero || currentHero.position !== targetHero.position;
    });

    if (needPositionFix) {
      // 处理位置调整逻辑
    }

    // 显示结果
    if (errors.length > 0) {
      message.warning(`阵容已应用，但有部分错误:\n${errors.join("\n")}`);
    } else {
      message.success(`阵容 "${lineup.name}" 已应用`);
    }

    // 刷新数据
    lastRefreshTime = 0;
    await refreshTeamInfo();
  } catch (error) {
    // 错误处理
  } finally {
    resetLineupState(lineup);
  }
}, 1000); // 1秒防抖
```

### 5.4 英雄拖拽调整

```javascript
const onDragStart = (event, hero) => {
  draggedHeroId.value = hero.heroId;
  event.dataTransfer.effectAllowed = "move";
  event.dataTransfer.setData("text/plain", JSON.stringify(hero));
};

const onDragEnd = () => {
  draggedHeroId.value = null;
  dragOverPosition.value = null;
};

const onDragOver = (event, hero) => {
  if (draggedHeroId.value !== hero.heroId) {
    dragOverPosition.value = hero.position;
  }
};

const onDragLeave = () => {
  dragOverPosition.value = null;
};

const onDrop = (event, targetHero) => {
  event.preventDefault();
  dragOverPosition.value = null;

  if (!draggedHeroId.value || draggedHeroId.value === targetHero.heroId) {
    return;
  }

  const draggedHero = editingHeroes.value.find(
    (h) => h.heroId === draggedHeroId.value,
  );
  if (!draggedHero) return;

  // 初始化编辑数据
  if (Object.keys(editingTeamHeroes.value).length === 0) {
    currentTeamHeroes.value.forEach((h) => {
      editingTeamHeroes.value[h.position] = {
        heroId: h.heroId,
        artifactId: h.artifactId || null,
        attachmentUid: h.attachmentUid || null,
      };
    });
  }

  // 交换位置
  const draggedPos = draggedHero.position;
  const targetPos = targetHero.position;

  const draggedHeroData = editingTeamHeroes.value[draggedPos];
  const targetHeroData = editingTeamHeroes.value[targetPos];

  editingTeamHeroes.value[draggedPos] = targetHeroData;
  editingTeamHeroes.value[targetPos] = draggedHeroData;

  message.success(
    `已将 ${getHeroName(draggedHero.heroId)} 与 ${getHeroName(targetHero.heroId)} 交换位置`,
  );

  draggedHeroId.value = null;
};
```

## 6. 使用说明

### 6.1 基本操作

1. **刷新数据**：点击"刷新数据"按钮获取最新的阵容和英雄信息
2. **保存阵容**：点击"保存阵容"按钮将当前阵容保存到本地存储
3. **上阵英雄**：点击"上阵英雄"按钮打开英雄选择界面，选择要上阵的英雄
4. **更换英雄**：点击英雄卡片上的"更换"按钮，选择要替换的英雄
5. **下阵英雄**：点击英雄卡片上的"下阵"按钮将英雄从阵容中移除
6. **调整位置**：拖拽英雄卡片可以调整英雄在阵容中的位置
7. **查看装备洗练**：点击英雄卡片查看装备洗练信息
8. **切换阵容槽位**：点击"阵容槽位"下方的按钮切换不同的阵容槽位
9. **管理已保存阵容**：点击"已保存阵容"按钮查看和管理已保存的阵容

### 6.2 阵容管理

1. **应用阵容**：在已保存阵容列表中点击"应用"按钮快速应用该阵容
2. **重命名阵容**：点击"重命名"按钮修改阵容名称
3. **删除阵容**：点击"删除"按钮删除不需要的阵容

### 6.3 英雄筛选

在英雄选择界面中，可以通过以下方式筛选英雄：
- **搜索**：在搜索框中输入英雄名称
- **品质筛选**：点击品质标签（红将、橙将、紫将）筛选对应品质的英雄
- **国家筛选**：点击国家标签（魏国、蜀国、吴国、群雄）筛选对应国家的英雄

## 7. 技术亮点

1. **响应式设计**：使用Vue 3的Composition API和响应式系统，实现数据与UI的自动同步
2. **WebSocket通信**：通过tokenStore与游戏服务器建立WebSocket连接，实时获取游戏数据
3. **拖拽功能**：实现了英雄位置的拖拽调整，提升用户体验
4. **本地存储**：使用localStorage存储已保存的阵容，确保数据持久化
5. **错误处理**：完善的错误处理和重试机制，提高操作成功率
6. **防抖优化**：对频繁操作（如应用阵容）进行防抖处理，避免服务器压力
7. **模块化设计**：代码结构清晰，功能模块化，易于维护和扩展

## 8. 注意事项

1. **WebSocket连接**：使用前需要确保WebSocket已连接，否则无法获取数据和执行操作
2. **Token选择**：需要先选择有效的Token才能使用阵容助手功能
3. **操作频率**：避免频繁操作，以免触发服务器限流
4. **数据同步**：阵容数据会保存在本地存储中，切换Token时会自动加载对应Token的阵容数据
5. **浏览器兼容性**：建议使用现代浏览器（Chrome、Firefox、Edge等）以获得最佳体验

## 9. 恢复备份说明

当需要恢复阵容助手组件时，需要确保以下文件存在：

1. **主组件文件**：`src/components/cards/Unlimitedlineup.vue`
2. **依赖文件**：
   - `src/stores/tokenStore.ts` - 用于WebSocket通信
   - `src/utils/HeroList.js` - 提供英雄和鱼灵数据
   - `src/components/Common/MyCard.vue` - 卡片组件

3. **依赖库**：
   - Vue 3
   - Naive UI
   - Pinia

确保以上文件和依赖都正确安装后，阵容助手组件即可正常工作。