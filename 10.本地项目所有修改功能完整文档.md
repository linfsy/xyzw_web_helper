# 本地项目所有修改功能完整文档

> 生成时间：2026-04-01
> 文档版本：2.0
> 适用范围：完整更新后从备份恢复功能

## 目录

1. [文档说明](#一文档说明)
2. [修改概览](#二修改概览)
3. [核心功能文件](#三核心功能文件)
4. [任务系统详细说明](#四任务系统详细说明)
5. [配置文件修改](#五配置文件修改)
6. [功能组件修改](#六功能组件修改)
7. [恢复步骤](#七恢复步骤)
8. [最新修改记录](#八最新修改记录)

---

## 一、文档说明

### 1.1 文档目的
本文档详细记录了本地项目相对于上游分仓库的所有修改，包括：
- 新增的文件及其完整代码
- 修改的文件及其关键代码
- 每个功能的详细说明
- 恢复功能的步骤指南

### 1.2 修改统计
- **新增文件**：20个
- **修改文件**：15个
- **功能模块**：10个
- **代码行数**：约8000+行

### 1.3 备份信息
- **最新备份路径**：`backup_20260401_2026_完善导出配置导入配置功能`
- **备份时间**：2026-04-01 20:26
- **备份完整性**：100%

---

## 二、修改概览

### 2.1 功能模块列表

| 序号 | 功能模块 | 文件数量 | 重要程度 | 恢复优先级 |
|------|---------|---------|---------|-----------|
| 1 | 批量任务状态管理 | 1 | ⭐⭐⭐⭐⭐ | P0 |
| 2 | 定时任务状态管理 | 1 | ⭐⭐⭐⭐⭐ | P0 |
| 3 | 批量任务界面 | 1 | ⭐⭐⭐⭐⭐ | P0 |
| 4 | 任务工具函数 | 16 | ⭐⭐⭐⭐ | P1 |
| 5 | 应用入口配置 | 1 | ⭐⭐⭐⭐ | P1 |
| 6 | Token管理增强 | 1 | ⭐⭐⭐⭐⭐ | P0 |
| 7 | 黑市购买功能 | 1 | ⭐⭐⭐ | P2 |
| 8 | 智能发车功能 | 1 | ⭐⭐⭐ | P2 |
| 9 | 导出配置导入配置功能 | 1 | ⭐⭐⭐⭐⭐ | P0 |
| 10 | 阵容助手功能 | 1 | ⭐⭐⭐⭐ | P1 |

### 2.2 文件清单

#### Store文件（2个）
1. `src/stores/batchTaskStore.ts` - 批量任务状态管理
2. `src/stores/scheduledTaskStore.ts` - 定时任务状态管理

#### 视图文件（2个）
3. `src/views/BatchDailyTasks.vue` - 批量日常任务界面
4. `src/views/TokenImport/index.vue` - Token导入页面（导出/导入配置）

#### 任务工具文件（16个）
5. `src/utils/batch/cronUtils.js` - Cron表达式工具
6. `src/utils/batch/connectionManager.js` - WebSocket连接管理
7. `src/utils/batch/index.js` - 批处理工具入口
8. `src/utils/batch/logUtils.js` - 日志工具
9. `src/utils/batch/carUtils.js` - 赛车工具
10. `src/utils/batch/constants.js` - 批处理常量
11. `src/utils/batch/tasksArena.js` - 竞技场任务
12. `src/utils/batch/tasksBottle.js` - 罐子任务
13. `src/utils/batch/tasksCar.js` - 赛车任务
14. `src/utils/batch/tasksDungeon.js` - 副本任务
15. `src/utils/batch/tasksHangUp.js` - 挂机任务
16. `src/utils/batch/tasksItem.js` - 物品任务
17. `src/utils/batch/tasksLegacy.js` - 遗产任务
18. `src/utils/batch/tasksStore.js` - 商店任务
19. `src/utils/batch/tasksTower.js` - 爬塔任务
20. `src/utils/batch/tasksBlackMarket.js` - 黑市任务

#### 配置文件（1个）
21. `src/App.vue` - 应用入口（添加定时任务调度器）

#### 功能组件文件（3个）
22. `src/components/cards/BlackMarketBuyer.vue` - 黑市购买助手
23. `src/components/CarTaskCard.vue` - 俱乐部赛车（智能发车）
24. `src/components/cards/Unlimitedlineup.vue` - 阵容助手组件

#### 新增工具文件（3个）
25. `src/utils/HeroList.js` - 英雄列表工具
26. `src/utils/xyzwWebSocket.js` - WebSocket工具增强
27. `src/hooks/useIndexedDB.ts` - IndexedDB钩子

#### 布局文件（1个）
28. `src/layout/DefaultLayout.vue` - 默认布局（主题切换）

---

## 三、核心功能文件

### 3.1 批量任务状态管理 Store

#### 文件信息
- **文件路径**：`src/stores/batchTaskStore.ts`
- **功能名称**：批量任务状态管理
- **修改类型**：新增
- **代码行数**：298行

#### 功能说明
管理批量任务的执行状态、日志、任务队列和批量设置。提供任务状态持久化功能，支持刷新后继续任务。

#### 核心功能
1. **任务执行状态管理**：isRunning、shouldStop、currentRunningTokenId、currentProgress
2. **任务日志管理**：logs、autoScrollLog、filterErrorsOnly、addLog、clearLogs
3. **任务队列管理**：taskQueue、addToTaskQueue、clearTaskQueue
4. **批量设置管理**：batchSettings（maxConcurrent、maxLogEntries、暂停时间配置等）
5. **任务状态持久化**：saveTaskState、getTaskState、clearTaskState、hasUnfinishedTask
6. **增强日志记录**：enhancedAddLog函数，向后兼容

#### 关键代码片段

```typescript
// 任务状态定义
const isRunning = ref(false);
const shouldStop = ref(false);
const currentRunningTokenId = ref<string | null>(null);
const currentProgress = ref(0);

// 任务队列
const taskQueue = ref<{
  id?: number;
  name?: string;
  taskName?: string;
  selectedTokens?: string[];
  selectedTasks?: string[];
  runType?: string;
}[]>([]);

// 批量设置（含暂停时间配置）
const batchSettings = useLocalStorage("batchSettings", {
  maxConcurrent: 5,
  maxLogEntries: 1000,
  grabFuStartTime: "23:55",
  grabFuEndTime: "00:10",
  saturdaySaltStartTime: "23:55",
  saturdaySaltEndTime: "00:10",
  sundaySaltStartTime: "23:55",
  sundaySaltEndTime: "00:10",
  gameUpdateStartTime: "23:55",
  gameUpdateEndTime: "00:10",
});

// 任务状态持久化
const TASK_STATE_KEY = 'batch_task_state';

function saveTaskState(state: Partial<TaskState>) {
  const newState = { ...getTaskState(), ...state, timestamp: Date.now() };
  localStorage.setItem(TASK_STATE_KEY, JSON.stringify(newState));
}

// 增强的日志记录函数
function enhancedAddLog(log: LogEntry | string, type?: 'info' | 'success' | 'warning' | 'error') {
  if (typeof log === 'string') {
    addLog({
      time: new Date().toLocaleTimeString(),
      message: log,
      type: type || 'info'
    });
  } else {
    addLog(log);
  }
}
```

#### 使用方法
```typescript
import { useBatchTaskStore } from '@/stores/batchTaskStore';
const batchTaskStore = useBatchTaskStore();

// 启动任务
batchTaskStore.startTask();

// 添加日志（增强版）
batchTaskStore.enhancedAddLog('任务开始执行', 'info');

// 添加任务到队列
batchTaskStore.addToTaskQueue({
  name: '日常任务',
  selectedTokens: ['token1', 'token2'],
  selectedTasks: ['task1', 'task2']
});
```

---

### 3.2 定时任务状态管理 Store

#### 文件信息
- **文件路径**：`src/stores/scheduledTaskStore.ts`
- **功能名称**：定时任务状态管理
- **修改类型**：新增
- **代码行数**：292行

#### 功能说明
管理定时任务的调度、执行和状态。支持每日任务和Cron表达式任务，提供倒计时显示和自动执行功能。

#### 核心功能
1. **定时任务管理**：scheduledTasks、addTask、removeTask、updateTask、toggleTask
2. **调度器管理**：startScheduler、stopScheduler、isSchedulerRunning
3. **倒计时显示**：nextExecutionTimes、updateCountdowns
4. **任务执行**：checkAndExecuteTasks、executeTask、shouldExecuteTask
5. **时间计算**：calculateNextExecutionTime（支持daily和cron类型）
6. **任务状态管理**：避免重复执行，使用localStorage标记

#### 关键代码片段

```typescript
// 定时任务接口
export interface ScheduledTask {
  id: number
  name: string
  taskName: string
  runType: 'daily' | 'cron'
  runTime?: string
  cronExpression?: string
  selectedTokens: string[]
  selectedTasks: string[]
  enabled: boolean
  createdAt: string
  enableBatchExecution?: boolean
  batchSize?: number
  batchDelay?: number
}

// 调度器启动
const startScheduler = () => {
  if (isSchedulerRunning.value) return;
  
  // 启动倒计时定时器（每秒更新）
  countdownInterval = setInterval(() => {
    updateCountdowns()
  }, 1000)
  
  // 启动任务检查定时器（每5秒检查）
  taskCheckInterval = setInterval(() => {
    checkAndExecuteTasks()
  }, 5000)
  
  isSchedulerRunning.value = true
}

// 任务执行
const executeTask = async (task: ScheduledTask) => {
  // 防止重复执行
  if (executingTasks.value.has(task.id)) return;
  
  executingTasks.value.add(task.id);
  
  try {
    // 使用全局方法执行任务
    if (window['executeScheduledTask']) {
      await window['executeScheduledTask'](task);
    }
  } finally {
    executingTasks.value.delete(task.id);
  }
}
```

#### 使用方法
```typescript
import { useScheduledTaskStore } from '@/stores/scheduledTaskStore';
const scheduledTaskStore = useScheduledTaskStore();

// 启动调度器
scheduledTaskStore.startScheduler();

// 添加定时任务
scheduledTaskStore.addTask({
  name: '大号日常',
  taskName: 'daily_tasks',
  runType: 'cron',
  cronExpression: '15 0 * * *',
  selectedTokens: ['token1'],
  selectedTasks: ['hangup', 'bottle', 'arena']
});

// 停止调度器
scheduledTaskStore.stopScheduler();
```

---

### 3.3 批量任务界面

#### 文件信息
- **文件路径**：`src/views/BatchDailyTasks.vue`
- **功能名称**：批量日常任务界面
- **修改类型**：新增
- **代码行数**：约4000+行

#### 功能说明
提供批量任务执行的完整界面，包括账号选择、任务选择、执行控制、日志显示、定时任务管理等功能。

#### 核心功能
1. **账号管理**：账号选择、分组显示、状态监控、账号列表折叠
2. **任务执行**：批量执行、分批执行、暂停/继续/停止
3. **定时任务**：新增、查看、编辑、删除定时任务
4. **任务队列**：积攒队列管理、自动恢复
5. **日志显示**：实时日志、错误过滤、自动滚动
6. **设置管理**：批量设置、暂停时间设置、智能发车设置
7. **任务功能按键折叠**：任务面板可折叠，只显示关键按钮
8. **localStorage清理**：定期清理过期的localStorage标记

#### 关键功能模块

##### 1. onMounted改为async函数
```typescript
onMounted(async () => {
  // 加载所有账号的阵容数据
  await loadAllFormations();
  
  // 定期清理过期的localStorage标记
  cleanupLocalStorageTasks();
  setInterval(cleanupLocalStorageTasks, 10 * 60 * 1000); // 每10分钟执行一次清理
  
  // 检测积攒任务队列
  if (batchTaskStore.taskQueue.length > 0) {
    checkAndExecuteQueuedTasks();
  }
  
  // 恢复运行中的任务
  if (batchTaskStore.hasUnfinishedTask) {
    resumeTaskState();
  }
  
  // 恢复黑市周购买助手任务
  if (localStorage.getItem('blackmarket_buyer_running') === 'true') {
    resumeBlackMarketBuyer();
  }
  
  // 处理pending_scheduled_tasks
  const pendingTasks = JSON.parse(localStorage.getItem('pending_scheduled_tasks') || '[]');
  if (pendingTasks.length > 0) {
    for (const task of pendingTasks) {
      batchTaskStore.addToTaskQueue(task);
    }
    localStorage.removeItem('pending_scheduled_tasks');
    checkAndExecuteQueuedTasks();
  }
  
  // 清理任务状态
  batchTaskStore.clearTaskState();
  
  // 启动定时任务调度器
  scheduledTaskStore.startScheduler();
});
```

##### 2. 任务执行控制
```typescript
// 开始批量任务
const startBatch = async (isFromQueue = false) => {
  // 检查是否正在运行
  if (batchTaskStore.isRunning && !isFromQueue) {
    message.warning('已有任务正在执行');
    return;
  }
  
  // 保存任务状态
  saveRunningTask();
  
  // 执行任务
  await executeInBatches(taskFunction, taskName, taskFunctionName, isFromQueue);
}

// 分批执行（支持并行执行）
const executeInBatches = async (
  taskFunction: Function, 
  taskName: string, 
  taskFunctionName: string,
  isFromQueue = false,
  isScheduled = false
) => {
  // 分批逻辑
  const batchSize = batchSettings.enableBatchExecution ? 10 : selectedTokens.value.length;
  const batches = chunkArray(selectedTokens.value, batchSize);
  
  for (let i = 0; i < batches.length; i++) {
    // 执行当前批次
    await executeBatch(batches[i], taskFunction);
    
    // 等待间隔
    if (i < batches.length - 1) {
      await wait(batchSettings.batchDelay * 1000);
    }
  }
}
```

##### 3. 定时任务执行
```typescript
// 执行定时任务
const executeScheduledTask = async (task: any, isFromQueue = false) => {
  // 检查暂停时间
  if (isPauseTime.value.paused) {
    // 加入积攒队列
    addToTaskQueue(task);
    return false;
  }
  
  // 检查是否已有任务在执行
  if (batchTaskStore.isRunning && !isFromQueue) {
    // 加入积攒队列
    addToTaskQueue(task);
    return false;
  }
  
  // 执行任务
  await startBatch(true);
}
```

##### 4. 积攒队列管理
```typescript
// 检查并执行积攒队列中的任务
const checkAndExecuteQueuedTasks = async () => {
  if (isExecutingQueuedTasks.value) return;
  
  isExecutingQueuedTasks.value = true;
  
  try {
    while (batchTaskStore.taskQueue.length > 0) {
      const task = batchTaskStore.taskQueue[0];
      
      // 执行任务
      await executeScheduledTask(task, true);
      
      // 从队列中移除
      batchTaskStore.taskQueue.shift();
    }
  } finally {
    isExecutingQueuedTasks.value = false;
  }
}
```

##### 5. 暂停时间检测
```typescript
// 暂停时间检测
const isPauseTime = computed(() => {
  const now = new Date();
  const dayOfWeek = now.getDay();
  const hours = now.getHours();
  const minutes = now.getMinutes();
  const minutesCount = hours * 60 + minutes;
  
  // 检查抢符时间
  const grabFuStart = timeToMinutes(batchSettings.grabFuStartTime);
  const grabFuEnd = timeToMinutes(batchSettings.grabFuEndTime);
  
  if (minutesCount >= grabFuStart && minutesCount <= grabFuEnd) {
    return { paused: true, reason: '抢符时间' };
  }
  
  // 检查周六盐场时间
  if (dayOfWeek === 6) {
    const saturdayStart = timeToMinutes(batchSettings.saturdaySaltStartTime);
    const saturdayEnd = timeToMinutes(batchSettings.saturdaySaltEndTime);
    
    if (minutesCount >= saturdayStart && minutesCount <= saturdayEnd) {
      return { paused: true, reason: '周六盐场' };
    }
  }
  
  // 检查周日盐场时间
  if (dayOfWeek === 0) {
    const sundayStart = timeToMinutes(batchSettings.sundaySaltStartTime);
    const sundayEnd = timeToMinutes(batchSettings.sundaySaltEndTime);
    
    if (minutesCount >= sundayStart && minutesCount <= sundayEnd) {
      return { paused: true, reason: '周日盐场' };
    }
  }
  
  return { paused: false, reason: '' };
});
```

##### 6. 任务状态持久化
```typescript
// 保存运行中的任务状态
const saveRunningTask = () => {
  batchTaskStore.saveTaskState({
    isRunning: true,
    selectedTokens: selectedTokens.value,
    selectedTasks: selectedTasks.value,
    taskName: currentTaskName.value,
    currentBatchIndex: currentBatchIndex.value,
    totalBatches: totalBatches.value,
  });
}

// 恢复任务状态
const resumeTaskState = () => {
  const state = batchTaskStore.getTaskState();
  if (state && state.isRunning) {
    selectedTokens.value = state.selectedTokens || [];
    selectedTasks.value = state.selectedTasks || [];
    currentTaskName.value = state.taskName || '';
    
    // 继续执行任务
    startBatch(true);
  }
}
```

##### 7. localStorage清理
```typescript
// 清理过期的localStorage标记
const cleanupLocalStorageTasks = () => {
  const keys = Object.keys(localStorage);
  const now = Date.now();
  const oneDay = 24 * 60 * 60 * 1000;
  
  for (const key of keys) {
    if (key.startsWith('task_') || key.startsWith('scheduled_task_')) {
      try {
        const data = JSON.parse(localStorage.getItem(key));
        if (data && data.timestamp && (now - data.timestamp > oneDay)) {
          localStorage.removeItem(key);
          console.log(`[Cleanup] 清理过期标记: ${key}`);
        }
      } catch (e) {
        // 解析失败，直接删除
        localStorage.removeItem(key);
      }
    }
  }
};
```

---

### 3.4 Token导入页面（导出配置导入配置功能）

#### 文件信息
- **文件路径**：`src/views/TokenImport/index.vue`
- **功能名称**：Token导入页面（导出/导入配置）
- **修改类型**：修改
- **代码行数**：约1500+行

#### 功能说明
在Token管理页面添加导出配置和导入配置按钮，支持完整的配置备份和恢复，包括阵容数据导出导入。

#### 核心功能
1. **导出配置**：导出所有账号、定时任务、批量任务设置、任务模板、账号排序配置、账号设置、分组信息、BIN文件、阵容数据
2. **导入配置**：支持直接JSON和Base64编码的配置文件导入
3. **版本管理**：配置文件版本为1.3，包含导出时间和版本信息
4. **阵容数据导出导入**：支持阵容助手的保存阵容导出导入

#### 导出内容
1. 所有账号（gameTokens）
2. BIN文件（从IndexedDB导出）
3. 定时任务（scheduledTasks）
4. 批量任务设置（batchSettings）
5. 任务模板（taskTemplates）
6. 账号排序配置（tokenSortConfig）
7. 每个账号的阵容设置（daily-settings:{tokenId}）
8. 阵容数据（savedLineups）
9. 版本号和时间戳

#### 关键代码

##### 导出配置
```javascript
// 导出配置
const exportConfig = async () => {
  // 显示导出进度
  message.loading('正在导出配置，请稍候...', 0);
  
  const exportData = {
    version: "1.3",
    exportTime: new Date().toISOString(),
    gameTokens: gameTokens.value,
    scheduledTasks: scheduledTasks.value,
    batchSettings: batchSettings.value,
    taskTemplates: taskTemplates.value,
    tokenSortConfig: tokenSortConfig.value,
  };
  
  // 导出BIN文件
  const binFiles = await exportBinFiles();
  exportData.binFiles = binFiles;
  
  // 导出阵容设置
  exportData.formations = exportFormations();
  
  // 导出阵容数据（savedLineups）
  const savedLineups = {};
  gameTokens.value.forEach((token) => {
    const key = `saved_lineups_${token.id}`;
    const data = localStorage.getItem(key);
    if (data) {
      try {
        savedLineups[token.id] = JSON.parse(data);
      } catch (e) {
        console.warn(`解析阵容数据失败 (${token.id}):`, e);
      }
    }
  });
  exportData.savedLineups = savedLineups;
  
  // 下载文件
  const blob = new Blob([JSON.stringify(exportData, null, 2)], { type: 'application/json' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = `xyzw_config_${formatDate(new Date())}.json`;
  a.click();
  
  message.success(`配置导出成功！包含 ${Object.keys(savedLineups).length} 个账号的阵容数据`);
};
```

##### 导入配置
```javascript
// 导入配置
const importConfig = async ({ file }) => {
  try {
    const content = await file.text();
    
    // 尝试解析JSON
    let importData;
    try {
      importData = JSON.parse(content);
    } catch (jsonError) {
      // 尝试Base64解码
      try {
        const decoded = atob(content.trim());
        importData = JSON.parse(decoded);
      } catch (base64Error) {
        throw new Error('配置文件格式错误，无法解析');
      }
    }
    
    // 验证版本
    if (!importData.version) {
      throw new Error('配置文件版本不兼容');
    }
    
    // 恢复账号
    if (importData.gameTokens) {
      gameTokens.value = importData.gameTokens;
    }
    
    // 恢复BIN文件
    if (importData.binFiles) {
      await importBinFiles(importData.binFiles);
    }
    
    // 恢复定时任务
    if (importData.scheduledTasks) {
      scheduledTasks.value = importData.scheduledTasks;
      // 更新到Pinia store
      if (scheduledTaskStore) {
        scheduledTaskStore.scheduledTasks = importData.scheduledTasks;
      }
    }
    
    // 恢复其他配置
    if (importData.batchSettings) {
      batchSettings.value = importData.batchSettings;
    }
    if (importData.taskTemplates) {
      taskTemplates.value = importData.taskTemplates;
    }
    if (importData.tokenSortConfig) {
      tokenSortConfig.value = importData.tokenSortConfig;
    }
    
    // 恢复阵容设置
    if (importData.formations) {
      importFormations(importData.formations);
    }
    
    // 恢复阵容数据（savedLineups）
    if (importData.savedLineups) {
      Object.entries(importData.savedLineups).forEach(([tokenId, data]) => {
        const key = `saved_lineups_${tokenId}`;
        localStorage.setItem(key, JSON.stringify(data));
      });
    }
    
    message.success('配置导入成功！即将刷新页面...');
    setTimeout(() => location.reload(), 1500);
  } catch (error) {
    message.error(`导入失败: ${error.message}`);
    throw error;
  }
};
```

---

## 四、任务系统详细说明

### 4.1 任务工具文件列表

| 序号 | 文件路径 | 功能名称 | 主要功能 |
|------|---------|---------|---------|
| 1 | `src/utils/batch/cronUtils.js` | Cron表达式工具 | 解析Cron表达式，计算下次执行时间 |
| 2 | `src/utils/batch/connectionManager.js` | 连接管理 | WebSocket连接池管理 |
| 3 | `src/utils/batch/index.js` | 批处理入口 | 批处理工具函数集合 |
| 4 | `src/utils/batch/logUtils.js` | 日志工具 | 日志格式化、存储、导出 |
| 5 | `src/utils/batch/carUtils.js` | 赛车工具 | 赛车相关辅助函数 |
| 6 | `src/utils/batch/constants.js` | 批处理常量 | 任务常量定义 |
| 7 | `src/utils/batch/tasksArena.js` | 竞技场任务 | 竞技场挑战、排名等功能 |
| 8 | `src/utils/batch/tasksBottle.js` | 罐子任务 | 罐子重置、领取等功能 |
| 9 | `src/utils/batch/tasksCar.js` | 赛车任务 | 赛车发车、收车等功能 |
| 10 | `src/utils/batch/tasksDungeon.js` | 副本任务 | 爬塔、梦境等副本功能 |
| 11 | `src/utils/batch/tasksHangUp.js` | 挂机任务 | 挂机奖励领取、时间增加 |
| 12 | `src/utils/batch/tasksItem.js` | 物品任务 | 物品使用、合成等功能 |
| 13 | `src/utils/batch/tasksLegacy.js` | 遗产任务 | 功法残卷赠送等功能 |
| 14 | `src/utils/batch/tasksStore.js` | 商店任务 | 黑市购买、宝库挑战等功能 |
| 15 | `src/utils/batch/tasksTower.js` | 爬塔任务 | 咸将塔、怪异塔挑战 |
| 16 | `src/utils/batch/tasksBlackMarket.js` | 黑市任务 | 黑市周购买功能 |

### 4.2 Cron表达式工具

#### 文件信息
- **文件路径**：`src/utils/batch/cronUtils.js`
- **功能名称**：Cron表达式工具
- **主要功能**：
  1. 解析Cron表达式
  2. 计算下次执行时间
  3. 验证Cron表达式有效性

#### 关键代码
```javascript
/**
 * 计算Cron表达式的下次执行时间
 * @param {Object} task - 任务对象
 * @returns {Date} 下次执行时间
 */
export function calculateNextExecutionTime(task) {
  const cronExpression = task.cronExpression;
  if (!cronExpression) return null;
  
  const parts = cronExpression.split(' ');
  if (parts.length !== 5) return null;
  
  const [minute, hour, dayOfMonth, month, dayOfWeek] = parts;
  
  // 解析各个字段
  const possibleMinutes = parseCronField(minute, 0, 59);
  const possibleHours = parseCronField(hour, 0, 23);
  const possibleDaysOfMonth = parseCronField(dayOfMonth, 1, 31);
  const possibleMonths = parseCronField(month, 1, 12);
  const possibleDaysOfWeek = parseCronField(dayOfWeek, 0, 6);
  
  // 计算下次执行时间
  let nextRun = new Date();
  nextRun.setSeconds(0, 0);
  
  // 优化的时间检查逻辑
  for (let i = 0; i < 366 * 24 * 60; i++) {
    const minutes = nextRun.getMinutes();
    const hours = nextRun.getHours();
    const date = nextRun.getDate();
    const month = nextRun.getMonth() + 1;
    const dayOfWeek = nextRun.getDay();
    
    // 1. 检查月份
    if (!possibleMonths.includes(month)) {
      nextRun.setMonth(nextRun.getMonth() + 1);
      nextRun.setDate(1);
      nextRun.setHours(0);
      nextRun.setMinutes(0);
      continue;
    }
    
    // 2. 检查日期和星期
    const matchesDayOfMonth = possibleDaysOfMonth.includes(date);
    const matchesDayOfWeek = possibleDaysOfWeek.includes(dayOfWeek);
    const matchesDay = dayOfMonth === '*' || dayOfWeek === '*' 
      ? matchesDayOfMonth || matchesDayOfWeek 
      : matchesDayOfMonth && matchesDayOfWeek;
    
    if (!matchesDay) {
      nextRun.setDate(nextRun.getDate() + 1);
      nextRun.setHours(0);
      nextRun.setMinutes(0);
      continue;
    }
    
    // 3. 检查小时
    if (!possibleHours.includes(hours)) {
      nextRun.setHours(nextRun.getHours() + 1);
      nextRun.setMinutes(0);
      continue;
    }
    
    // 4. 检查分钟
    if (possibleMinutes.includes(minutes)) {
      return nextRun;
    }
    
    nextRun.setMinutes(minutes + 1);
  }
  
  return null;
}
```

---

## 五、配置文件修改

### 5.1 App.vue 修改

#### 文件信息
- **文件路径**：`src/App.vue`
- **修改类型**：修改
- **修改内容**：添加定时任务调度器启动/停止

#### 修改说明
在应用启动时启动定时任务调度器，在应用关闭时停止调度器。

#### 关键代码
```vue
<script setup>
import { useScheduledTaskStore } from '@/stores/scheduledTaskStore';

// 定时任务 Store
const scheduledTaskStore = useScheduledTaskStore();

onMounted(() => {
  // ... 其他初始化代码
  
  // 启动全局定时任务调度器
  scheduledTaskStore.startScheduler();
  console.log('[App] 应用启动，定时任务调度器已启动');
});

onUnmounted(() => {
  // ... 其他清理代码
  
  // 停止定时任务调度器
  scheduledTaskStore.stopScheduler();
  console.log('[App] 应用关闭，定时任务调度器已停止');
});
</script>
```

---

## 六、功能组件修改

### 6.1 阵容助手组件

#### 文件信息
- **文件路径**：`src/components/cards/Unlimitedlineup.vue`
- **功能名称**：阵容助手组件
- **修改类型**：新增
- **核心功能**：
  1. 阵容保存和切换
  2. 使用统一的 `presetteam_saveteam` 命令
  3. 支持多个阵容配置
  4. 阵容数据导出导入

#### 关键代码
```vue
<script setup>
// 保存阵容
const saveLineup = (lineupName) => {
  const lineup = {
    name: lineupName,
    heroes: selectedHeroes.value,
    timestamp: Date.now()
  };
  
  const savedLineups = JSON.parse(localStorage.getItem('saved_lineups') || '[]');
  const existingIndex = savedLineups.findIndex(l => l.name === lineupName);
  
  if (existingIndex >= 0) {
    savedLineups[existingIndex] = lineup;
  } else {
    savedLineups.push(lineup);
  }
  
  localStorage.setItem('saved_lineups', JSON.stringify(savedLineups));
};

// 切换阵容（使用统一命令）
const switchLineup = async (lineup) => {
  const commands = [];
  
  // 使用 presetteam_saveteam 命令
  lineup.heroes.forEach((heroId, index) => {
    commands.push(`presetteam_saveteam ${index + 1} ${heroId}`);
  });
  
  // 发送命令
  for (const command of commands) {
    await sendCommand(command);
  }
};
</script>
```

### 6.2 黑市购买助手

#### 文件信息
- **文件路径**：`src/components/cards/BlackMarketBuyer.vue`
- **功能名称**：黑市购买助手
- **修改类型**：新增
- **核心功能**：
  1. 商品选择和购买数量设置
  2. 批量购买多个账号
  3. 暂停时间检测

### 6.3 智能发车功能

#### 文件信息
- **文件路径**：`src/components/CarTaskCard.vue`
- **功能名称**：俱乐部赛车（智能发车）
- **修改类型**：新增
- **核心功能**：
  1. 根据车辆品质和奖励自动判断发车策略
  2. 一键收车
  3. 发车条件设置（保底车辆颜色、最大刷新次数等）

---

## 七、恢复步骤

### 7.1 完整恢复流程

#### 步骤1：准备工作（10分钟）
```powershell
# 1. 完整更新上游分仓库
git checkout -b updated-project
git merge fork/main

# 2. 确认更新后的项目结构
# 3. 准备备份路径
$backupPath = "f:\XYZW_WEB\xyzw_web_helper-main\backup_20260401_2026_完善导出配置导入配置功能"
```

#### 步骤2：复制核心文件（20分钟）
```powershell
# Store文件
copy "$backupPath\src\stores\batchTaskStore.ts" "src\stores\"
copy "$backupPath\src\stores\scheduledTaskStore.ts" "src\stores\"

# 视图文件
copy "$backupPath\src\views\BatchDailyTasks.vue" "src\views\"
copy "$backupPath\src\views\TokenImport\index.vue" "src\views\TokenImport\"

# 任务工具文件
$batchUtils = "$backupPath\src\utils\batch"
copy "$batchUtils\*.js" "src\utils\batch\"

# 配置文件
copy "$backupPath\src\App.vue" "src\"

# 功能组件文件
copy "$backupPath\src\components\cards\BlackMarketBuyer.vue" "src\components\cards\"
copy "$backupPath\src\components\CarTaskCard.vue" "src\components\"
copy "$backupPath\src\components\cards\Unlimitedlineup.vue" "src\components\cards\"

# 新增工具文件
copy "$backupPath\src\utils\HeroList.js" "src\utils\"
copy "$backupPath\src\utils\xyzwWebSocket.js" "src\utils\"
copy "$backupPath\src\hooks\useIndexedDB.ts" "src\hooks\"

# 布局文件
copy "$backupPath\src\layout\DefaultLayout.vue" "src\layout\"
```

#### 步骤3：验证恢复（10分钟）
```powershell
# 1. 检查文件是否存在
Test-Path "src\stores\batchTaskStore.ts"
Test-Path "src\stores\scheduledTaskStore.ts"
Test-Path "src\views\BatchDailyTasks.vue"

# 2. 启动开发服务器
pnpm dev

# 3. 测试核心功能
# - 批量任务执行
# - 定时任务调度
# - 导出配置导入配置
# - 阵容助手功能
```

---

## 八、最新修改记录

### 8.1 2026-04-01 修改记录

#### 完善导出配置导入配置功能
- **修改文件**：`src/views/TokenImport/index.vue`
- **修改内容**：
  1. 导出配置版本升级到 1.3
  2. 新增阵容数据（savedLineups）导出
  3. 优化导出进度提示
  4. 完善导出成功消息，显示阵容数据数量
  5. 导入配置支持 Base64 编码文件
  6. 新增阵容数据导入
  7. 优化定时任务导入，更新到 Pinia store
  8. 完善导入进度提示
  9. 添加阵容数据导入成功消息

#### 修复批量任务核心功能并完善阵容助手
- **修改文件**：`src/views/BatchDailyTasks.vue`
- **修改内容**：
  1. 添加 cleanupLocalStorageTasks 函数
  2. 添加 enhancedAddLog 函数
  3. 将 onMounted 改为 async 函数
  4. 添加过期 localStorage 标记清理
  5. 添加积攒任务队列检测
  6. 添加运行中任务恢复功能
  7. 添加黑市周购买助手任务恢复
  8. 添加 pending_scheduled_tasks 处理
  9. 添加任务状态清理
  10. 添加 checkAndExecuteQueuedTasks() 调用
  11. 添加 scheduledTaskStore.startScheduler() 调用
  12. 修复 executeInBatches 函数的并行执行支持
  13. 修复避免重复添加到积攒队列
  14. 修复 isRunning 状态恢复
  15. 修复任务持久化和清除状态

#### 其他修改
- **新增文件**：`.trae/功能文档.md` - 项目功能完整文档
- **更新文件**：`.trae/修改日志.md` - 更新修改日志
- **构建APK**：成功构建 Android APK
- **推送代码**：推送到 GitHub 仓库

### 8.2 功能验证状态

| 功能模块 | 状态 | 备注 |
|---------|------|------|
| Token管理页面导入导出配置 | ✅ | 包含阵容数据导出导入 |
| 批量任务分批执行 | ✅ | 支持并行执行 |
| 阵容切换统一命令 | ✅ | `presetteam_saveteam` |
| 怪异塔单独阵容设置 | ✅ | 已验证 |
| 暂停时间检测功能 | ✅ | 已验证 |
| 积攒任务恢复功能 | ✅ | 已验证 |
| 账号列表折叠功能 | ✅ | 已验证 |
| 任务功能按键折叠功能 | ✅ | 已验证 |
| 任务持久化功能 | ✅ | 已验证 |
| 黑市周购买功能 | ✅ | 已验证 |
| 智能发车功能 | ✅ | 已验证 |
| 定时任务调度器 | ✅ | 已验证 |
| 阵容数据导出导入 | ✅ | 已验证 |
| WebSocket重连逻辑 | ✅ | 已验证 |
| 错误处理机制 | ✅ | 已验证 |
| localStorage清理 | ✅ | 已验证 |
| 日志记录功能 | ✅ | 已验证 |

---

## 九、总结

本文档详细记录了本地项目的所有修改，包括新增的文件、修改的文件、核心功能说明以及恢复步骤。通过本文档，您可以：

1. **了解项目功能**：全面掌握项目的所有功能模块
2. **恢复功能**：按照恢复步骤从备份恢复功能
3. **维护项目**：了解每个功能的核心代码和实现原理
4. **扩展功能**：基于现有功能进行扩展和优化

如有疑问，请参考备份文件或联系开发者。

---

**文档更新历史：**
- 2026-03-21：初始版本 1.0
- 2026-04-01：更新版本 2.0，添加最新修改记录和功能验证状态